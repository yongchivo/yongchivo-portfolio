---
import BaseLayout from "../layouts/BaseLayout.astro";
import TimeLineElement from "../components/cv/TimeLine.astro";
import {
  profile,
  education,
  projects,
  academicProjects,
  experience,
  certifications,
  skills,
} from "../data/cv";
---

<BaseLayout title="CV" sideBarActiveItemID="cv">
  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Profile</div>
  </div>

  <div class="mb-10 text-justify">
    {profile}
  </div>

  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Projects</div>
  </div>

  <div class="time-line-container mb-10">
    {
      projects.map((item) => (
        <TimeLineElement title={item.title} subtitle={item.subtitle}>
          {item.body}
        </TimeLineElement>
      ))
    }
  </div>

  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Education</div>
  </div>

  <div class="time-line-container grid gap-4 mb-10">
    {
      education.map((item) => (
        <TimeLineElement title={item.title} subtitle={item.subtitle}>
          {item.body}
        </TimeLineElement>
      ))
    }
  </div>

  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Academic Projects</div>
  </div>

  <div class="time-line-container mb-10">
    {
      academicProjects.map((item) => (
        <TimeLineElement title={item.title} subtitle={item.subtitle}>
          {item.body}
        </TimeLineElement>
      ))
    }
  </div>

  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Experience</div>
  </div>

  <div class="time-line-container mb-10">
    {
      experience.map((item) => (
        <TimeLineElement title={item.title} subtitle={item.subtitle}>
          {item.body}
        </TimeLineElement>
      ))
    }
  </div>

  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Certifications & Licences</div>
  </div>

  <ul class="list-disc mx-6 mb-10 grid gap-2">
    {certifications.map((item) => <li>{item}</li>)}
  </ul>

  <div class="mb-5">
    <div class="text-3xl w-full font-bold">Skills</div>
  </div>

  {
    skills.map((group) => (
      <div class="mb-5">
        <div class="text-xl font-bold mb-2">{group.name}</div>
        <ul class="list-disc md:columns-3 columns-2 mx-6">
          {group.items.map((item) => (
            <li>{item}</li>
          ))}
        </ul>
      </div>
    ))
  }
</BaseLayout>
